{
  "name": "blindtest",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev:server": "npm -w @blindtest/server run dev",
    "dev:client": "npm -w @blindtest/client run dev",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "build:shared": "npm -w @blindtest/shared run build",
    "build": "npm run build:shared && npm -w @blindtest/server run build && npm -w @blindtest/client run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
